[project]
name = "hiver-support-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pandas", "pyarrow", "httpx", "numpy", "scikit-learn"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
